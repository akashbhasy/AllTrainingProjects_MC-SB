spring:
 profiles:
  include: native,default
 cloud:  config:   discovery:
    enabled: false
   server:
    native:
     searchLocations: D:/ust/ProjectDetails
    encrypt:           enabled: true
server:
 port: 8888        